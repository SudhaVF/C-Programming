#include<stdio.h>
int p, q, r, s;
int main(){

    p = 2;
    q = 5;
    r = 7;
    s = p*q*r;

    printf("P = %d",p);
    printf("\nq = %d",q);
    printf("\nr = %d",r);
    printf("\ns = %d",s);
}